import 'package:flutter/material.dart';

class DisabilityVerPage extends StatelessWidget {
  const DisabilityVerPage ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Disability'),
      ),
    );
  }
}