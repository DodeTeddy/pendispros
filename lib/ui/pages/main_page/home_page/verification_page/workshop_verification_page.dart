import 'package:flutter/material.dart';

class WorkshopVerPage extends StatelessWidget {
  const WorkshopVerPage ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Workshop'),
      ),
    );
  }
}